FROM python:3.11-slim

WORKDIR /app

COPY strata/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY strata/ ./strata/

CMD ["celery", "-A", "strata.celery_app", "worker", "--loglevel=info", "--concurrency=2"]
