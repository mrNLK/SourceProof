# 20 — Add Loading Skeletons

Replace all loading spinners with skeleton loaders that match the layout of the content being loaded. For strategy generation: skeleton of the repos list, companies list, skills list, query box. For search results: skeleton of 6 candidate cards. For candidate profile expand: skeleton matching the profile layout. Use CSS animation (pulse/shimmer) on the skeletons. This prevents layout shift and feels faster.
